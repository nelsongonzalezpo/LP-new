# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LP-Exercise' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LP-Exercise

  target 'LP-ExerciseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LP-ExerciseUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  pod 'Alamofire'

end
